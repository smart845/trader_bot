# 🤖 QuantumTrader Telegram Bot

**QuantumTrader** — это Telegram-бот для анализа криптовалютного рынка и предоставления торговых рекомендаций (LONG/SHORT/HOLD) на основе продвинутых технических индикаторов и метрик.

Бот использует данные с биржи Binance (через публичные API) для получения свечных данных, фандинг-рейта и открытого интереса, а затем применяет сложный алгоритм технического анализа.

## 🚀 Деплой на Render

Этот проект настроен для простого деплоя на платформу [Render](https://render.com) с использованием WebHook.

### Шаг 1: Получение токена бота

1.  Создайте нового бота через [@BotFather](https://t.me/BotFather) в Telegram.
2.  Сохраните полученный **HTTP API Token**.

### Шаг 2: Настройка Render

1.  **Создайте репозиторий на GitHub** и загрузите в него все файлы проекта (`main.py`, `app.py`, `requirements.txt`, `Procfile`, `README.md`).
2.  Перейдите на [Render Dashboard](https://dashboard.render.com/).
3.  Нажмите **New Web Service** и выберите ваш GitHub репозиторий.
4.  **Настройки сервиса:**
    *   **Name:** `quantum-trader-bot` (или любое другое имя)
    *   **Region:** Выберите ближайший регион.
    *   **Branch:** `main` (или ваша основная ветка)
    *   **Root Directory:** (Оставьте пустым)
    *   **Runtime:** `Python 3`
    *   **Build Command:** `pip install -r requirements.txt`
    *   **Start Command:** `uvicorn app:app --host 0.0.0.0 --port $PORT`
5.  **Переменные окружения (Environment Variables):**
    *   Добавьте переменную с ключом `TELEGRAM_BOT_TOKEN` и значением, полученным от BotFather.
6.  Нажмите **Create Web Service**.

### Шаг 3: Установка WebHook

После успешного деплоя Render предоставит вам публичный URL (например, `https://quantum-trader-bot.onrender.com`).

Вам нужно сообщить Telegram, куда отправлять обновления. Используйте следующую команду, заменив `YOUR_BOT_TOKEN` и `YOUR_RENDER_URL`:

```bash
# Замените YOUR_BOT_TOKEN и YOUR_RENDER_URL
# URL для WebHook должен быть YOUR_RENDER_URL/webhook
curl -F "url=YOUR_RENDER_URL/webhook" "https://api.telegram.org/botYOUR_BOT_TOKEN/setWebhook"
```

**Пример:**
Если ваш токен `12345:ABC-DEF` и URL Render `https://my-bot.onrender.com`, команда будет:
```bash
curl -F "url=https://my-bot.onrender.com/webhook" "https://api.telegram.org/bot12345:ABC-DEF/setWebhook"
```

После выполнения этой команды ваш бот начнет работать!

## ⚙️ Использование бота

После запуска бота в Telegram:

*   Начните работу с командой `/start`.
*   Получите прогноз, используя `/analyze <COIN>`, например: `/analyze BTC` или `/analyze SOL`.

## ⚠️ Важное примечание

Алгоритм анализа основан на **техническом анализе** и использует публичные данные Binance. Это не является финансовой рекомендацией. Используйте бота для образовательных целей и как дополнительный инструмент в вашей торговой стратегии.
